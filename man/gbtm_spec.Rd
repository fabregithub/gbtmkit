% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{gbtm_spec}
\alias{gbtm_spec}
\title{Create a group-based trajectory model specification}
\usage{
gbtm_spec(
  data,
  outcomes,
  time,
  id = NULL,
  family = gbtm_families(),
  covariates = NULL,
  ymin = NULL,
  ymax = NULL,
  ssigma = FALSE
)
}
\arguments{
\item{data}{A data frame (or matrix with column names) in wide format: one
row per subject, one column per outcome occasion.}

\item{outcomes}{Character vector of outcome column names, in time order.}

\item{time}{Character vector of time/occasion column names, the same length
as \code{outcomes}.}

\item{id}{Optional name of a subject-identifier column. If \code{NULL}, row
numbers are used. When supplied it must contain no duplicates.}

\item{family}{Outcome family; one of \code{\link[=gbtm_families]{gbtm_families()}}. \code{"binomial"} for
binary outcomes (LOGIT), \code{"gaussian"} for continuous (CNORM), \code{"poisson"}
for counts, \code{"beta"} for proportions.}

\item{covariates}{Optional character vector of column names used as
class-membership covariates (Nagin's "risk factors"): time-stable
subject-level variables that predict \emph{which group a subject belongs to}
via a multinomial model, without affecting the group trajectories
themselves. Supported by every engine (trajeR \code{Risk}, flexmix concomitant
model, lcmm \code{classmb}). Columns may be numeric, logical, or
factor/character (expanded via \code{\link[stats:model.matrix]{stats::model.matrix()}} where the engine
needs a numeric design) and must contain no missing values.}

\item{ymin, ymax}{Optional censoring bounds for continuous (\code{"gaussian"})
outcomes; passed through to engines that support censored-normal models.}

\item{ssigma}{Logical; for continuous outcomes, whether the residual
variance is shared across groups. Default \code{FALSE}.}
}
\value{
An object of class \code{gbtm_spec}: a list with the validated \code{data},
\code{outcomes}, \code{time}, \code{id}, \code{family}, \code{covariates}, and options.
}
\description{
Builds a validated \code{gbtm_spec} object describing the data and the model to
fit, without committing to an estimation engine. Columns are selected \emph{by
name}, and the outcome values are checked against the declared \code{family}.
}
\examples{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(
  sim_binary,
  outcomes = paste0("y", 1:10),
  time     = paste0("t", 1:10),
  id       = "id",
  family   = "binomial"
)
spec
}
