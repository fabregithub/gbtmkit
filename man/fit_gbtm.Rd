% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_gbtm}
\alias{fit_gbtm}
\title{Fit the final chosen trajectory model}
\usage{
fit_gbtm(
  spec,
  n_groups,
  degrees,
  method = NULL,
  engine = gbtm_engines(),
  hessian = TRUE,
  itermax = 100L,
  seed = NULL,
  ...
)
}
\arguments{
\item{spec}{A \link{gbtm_spec}.}

\item{n_groups}{Number of latent groups.}

\item{degrees}{Integer vector of polynomial degrees, length \code{n_groups}.}

\item{method}{Estimation method; must be one of \code{\link[=gbtm_engine_methods]{gbtm_engine_methods()}} for
the chosen engine (ignored by engines with a single optimizer).}

\item{engine}{Engine name; see \code{\link[=gbtm_engines]{gbtm_engines()}}.}

\item{hessian}{Logical; compute the Hessian (standard errors). Default \code{TRUE}.}

\item{itermax}{Maximum optimizer iterations.}

\item{seed}{Optional integer seed for reproducibility.}

\item{...}{Passed on to the underlying engine call.}
}
\value{
A \link{gbtm_fit} object.
}
\description{
A thin wrapper over \code{\link[=gbtm_fit]{gbtm_fit()}} intended for the final model, once the number
of groups and the polynomial shape have been chosen. It differs only in
defaulting \code{hessian = TRUE}, so the result includes standard errors.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, paste0("y", 1:10),
                  paste0("t", 1:10), id = "id", family = "binomial")
if (requireNamespace("trajeR", quietly = TRUE))
  fit_gbtm(spec, n_groups = 4, degrees = rep(3, 4), method = "L")
}
}
\seealso{
\code{\link[=gbtm_fit]{gbtm_fit()}}, \code{\link[=gbtm_diagnostics]{gbtm_diagnostics()}}, \code{\link[=gbtm_assign]{gbtm_assign()}},
\code{\link[=plot_trajectories]{plot_trajectories()}}
}
