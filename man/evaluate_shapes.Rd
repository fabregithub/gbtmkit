% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate-shapes.R
\name{evaluate_shapes}
\alias{evaluate_shapes}
\title{Stage 3: search polynomial shapes for a fixed number of groups}
\usage{
evaluate_shapes(
  spec,
  n_groups,
  engine = gbtm_engines(),
  method = NULL,
  strategy = c("stepwise", "grid"),
  min_degree = 1L,
  max_degree = 3L,
  max_passes = 2L,
  hessian = FALSE,
  itermax = 100L,
  seed = NULL,
  time_budget = Inf,
  max_fits = Inf,
  checkpoint = NULL,
  verbose = TRUE
)
}
\arguments{
\item{spec}{A \link{gbtm_spec}.}

\item{n_groups}{Number of groups.}

\item{engine}{Engine name; see \code{\link[=gbtm_engines]{gbtm_engines()}}.}

\item{method}{Estimation method (e.g. the winner of \code{\link[=select_algorithm]{select_algorithm()}}).}

\item{strategy}{\code{"stepwise"} (default, greedy coordinate descent) or \code{"grid"}
(full Cartesian product of degrees).}

\item{min_degree, max_degree}{Per-group polynomial degree bounds.}

\item{max_passes}{Maximum coordinate-descent passes for \code{"stepwise"}.}

\item{hessian}{Logical; keep \code{FALSE} during search (default).}

\item{itermax, seed}{Passed to \code{\link[=gbtm_fit]{gbtm_fit()}}.}

\item{time_budget}{Wall-clock limit: seconds, or a string like \code{"2h"}.
\code{Inf} for no limit.}

\item{max_fits}{Maximum number of model fits. \code{Inf} for no limit.}

\item{checkpoint}{Optional file path; results are appended here and a rerun
resumes, skipping shapes already evaluated.}

\item{verbose}{Print the run-time estimate and progress messages.}
}
\value{
An object of class \code{gbtm_shapes}: \verb{$table} (one row per shape),
\verb{$best} (degrees with the lowest BIC), \verb{$best_fit}, \verb{$n_fits},
\verb{$budget_hit}, and \verb{$strategy}.
}
\description{
Fits candidate per-group polynomial degrees and records the GRoLTS
diagnostics for each, returning one row per evaluated shape. The search runs
unattended and bounded: a greedy \code{"stepwise"} strategy by default, an
optional hard \code{time_budget}/\code{max_fits}, on-disk \code{checkpoint}ing with resume,
and an up-front run-time estimate.
}
\seealso{
\code{\link[=apply_grolts_criteria]{apply_grolts_criteria()}}
}
