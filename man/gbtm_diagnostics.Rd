% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{gbtm_diagnostics}
\alias{gbtm_diagnostics}
\title{GRoLTS classification diagnostics for a fitted model}
\usage{
gbtm_diagnostics(fit, ...)
}
\arguments{
\item{fit}{A \link{gbtm_fit} object.}

\item{...}{Unused.}
}
\value{
An object of class \code{gbtm_diagnostics}: a list with \code{groups} (a
per-group data frame), scalar \code{entropy}, \code{n}, and \code{n_groups}. When called
on a fit it also carries \code{bic}, \code{aic}, \code{loglik}, and \code{degrees}.
}
\description{
Computes the group-based trajectory fit diagnostics used by the GRoLTS
checklist -- assigned and model-implied group proportions and their mismatch,
average posterior probability of assignment (APPA), odds of correct
classification (OCC), and the normalized classification entropy -- entirely
from the posterior matrix and model group sizes, so the result is identical
regardless of estimation engine.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, c("y1", "y2", "y3", "y4"),
                  c("t1", "t2", "t3", "t4"), id = "id", family = "binomial")
if (requireNamespace("trajeR", quietly = TRUE)) {
  fit <- gbtm_fit(spec, n_groups = 4, degrees = rep(1, 4), seed = 1)
  gbtm_diagnostics(fit)
}
}
}
