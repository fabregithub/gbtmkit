% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_gbtm_pipeline}
\alias{run_gbtm_pipeline}
\title{Run the full group-based trajectory pipeline}
\usage{
run_gbtm_pipeline(
  spec,
  engine = gbtm_engines(),
  candidates = 2:6,
  degree = 3L,
  method = NULL,
  algo_n_groups = NULL,
  algo_degree = NULL,
  strategy = c("stepwise", "grid"),
  min_degree = 1L,
  max_degree = 3L,
  max_passes = 2L,
  pms_min = 0.05,
  appa_min = 0.7,
  occ_min = 5,
  itermax = 100L,
  seed = NULL,
  time_budget = Inf,
  max_fits = Inf,
  checkpoint = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{spec}{A \link{gbtm_spec}.}

\item{engine}{Engine name; see \code{\link[=gbtm_engines]{gbtm_engines()}}.}

\item{candidates}{Integer vector of group numbers to consider (stage 2).}

\item{degree}{Polynomial degree used during group-number selection.}

\item{method}{Estimation method. If \code{NULL} and the engine offers a choice,
stage 1 selects it; otherwise this method is used throughout.}

\item{algo_n_groups, algo_degree}{Group count and degree used for stage-1
algorithm selection (defaults: \code{max(candidates)}, \code{degree}).}

\item{strategy, min_degree, max_degree, max_passes}{Shape-search controls, passed
to \code{\link[=evaluate_shapes]{evaluate_shapes()}}.}

\item{pms_min, appa_min, occ_min}{GRoLTS thresholds, passed to
\code{\link[=apply_grolts_criteria]{apply_grolts_criteria()}}.}

\item{itermax, seed}{Passed to the fitting stages.}

\item{time_budget, max_fits, checkpoint}{Bounds for the shape search.}

\item{verbose}{Print progress messages.}

\item{...}{Passed to the underlying fitting calls.}
}
\value{
An object of class \code{gbtm_result} with elements \code{spec}, \code{engine},
\code{method}, \code{algorithm_selection}, \code{group_selection}, \code{n_groups}, \code{shapes},
\code{criteria}, \code{chosen_degrees}, \code{criteria_met}, \code{final_fit}, \code{assignment},
\code{diagnostics}, and \code{call}.
}
\description{
Executes algorithm selection (optional), group-number selection, polynomial
shape search with GRoLTS acceptance criteria, and the final Hessian-on fit,
returning all intermediate results in one object.
}
\details{
If no shape meets the GRoLTS criteria, the pipeline falls back to the
lowest-BIC shape and records \code{criteria_met = FALSE}.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, c("y1","y2","y3","y4"),
                  c("t1","t2","t3","t4"), id = "id", family = "binomial")
if (requireNamespace("trajeR", quietly = TRUE)) {
  res <- run_gbtm_pipeline(spec, candidates = 2:5, method = "L")
  res
}
}
}
\seealso{
\code{\link[=select_algorithm]{select_algorithm()}}, \code{\link[=select_n_groups]{select_n_groups()}}, \code{\link[=evaluate_shapes]{evaluate_shapes()}},
\code{\link[=apply_grolts_criteria]{apply_grolts_criteria()}}, \code{\link[=fit_gbtm]{fit_gbtm()}}
}
