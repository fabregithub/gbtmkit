% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grolts-criteria.R
\name{apply_grolts_criteria}
\alias{apply_grolts_criteria}
\title{Apply GRoLTS acceptance criteria to a shape table}
\usage{
apply_grolts_criteria(
  shapes,
  pms_min = 0.05,
  appa_min = 0.7,
  occ_min = 5,
  order_by = "bic"
)
}
\arguments{
\item{shapes}{A \code{gbtm_shapes} object or its \verb{$table} data frame (from
\code{\link[=evaluate_shapes]{evaluate_shapes()}}).}

\item{pms_min}{Minimum smallest-group assigned proportion (default \code{0.05}).}

\item{appa_min}{Minimum average posterior probability of assignment
(default \code{0.70}).}

\item{occ_min}{Minimum odds of correct classification (default \code{5}).}

\item{order_by}{Column to sort survivors by, ascending (default \code{"bic"}).}
}
\value{
A data frame of surviving shapes ordered by \code{order_by}, carrying an
attribute \code{"recommended"} (its first row, or \code{NULL} if none qualify) and
\code{"thresholds"}. Class \code{gbtm_criteria}.
}
\description{
Keeps the candidate shapes whose worst group satisfies all of the GRoLTS
adequacy thresholds and orders the survivors by the chosen criterion. The
recommended model is the top row.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, paste0("y", 1:10),
                  paste0("t", 1:10), id = "id", family = "binomial")
if (requireNamespace("trajeR", quietly = TRUE)) {
  sh <- evaluate_shapes(spec, n_groups = 4, method = "L", verbose = FALSE)
  apply_grolts_criteria(sh)
}
}
}
