% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_engines}
\alias{benchmark_engines}
\title{Benchmark the estimation engines on a specification}
\usage{
benchmark_engines(
  spec,
  n_groups,
  degrees,
  engines = gbtm_engines(),
  method = NULL,
  hessian = FALSE,
  itermax = 100L,
  seed = NULL,
  ...
)
}
\arguments{
\item{spec}{A \link{gbtm_spec}.}

\item{n_groups}{Number of latent groups.}

\item{degrees}{Integer vector of polynomial degrees, length \code{n_groups}.
Engines without per-group degrees (see
\code{\link[=gbtm_engine_per_group_degrees]{gbtm_engine_per_group_degrees()}}) are skipped -- with a note -- unless
the degrees are uniform.}

\item{engines}{Engines to benchmark; defaults to all registered engines.
Engines whose package is not installed or whose families do not include
\code{spec$family} are recorded as skipped.}

\item{method}{Estimation method for engines that offer a choice (trajeR);
ignored by single-optimiser engines.}

\item{hessian, itermax, seed, ...}{Passed to \code{\link[=gbtm_fit]{gbtm_fit()}} (e.g. \code{n_starts}).}
}
\value{
An object of class \code{gbtm_benchmark}: a data frame with one row per
engine (\code{engine}, \code{ok}, \code{seconds}, \code{bic}, \code{loglik}, \code{entropy},
\code{min_appa}, \code{groups_effective}, \code{note}).
}
\description{
Fits the same model with each requested engine and reports wall-clock time
alongside the engine-neutral diagnostics (entropy, minimum APPA, effective
number of groups). Use it to pick a backend for a large problem: fit a
subsample first, then commit to the fastest engine whose classification
quality is adequate.
}
\details{
BIC and log-likelihood are reported for completeness but are comparable
only \emph{within} an engine -- each backend defines its likelihood differently.
Compare engines on time and on the classification diagnostics.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, paste0("y", 1:10),
                  paste0("t", 1:10), id = "id", family = "binomial")
benchmark_engines(spec, n_groups = 4, degrees = rep(2, 4), seed = 1)
}
}
