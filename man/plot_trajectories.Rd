% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_trajectories}
\alias{plot_trajectories}
\title{Plot fitted group trajectories}
\usage{
plot_trajectories(fit, observed = TRUE, n = 100L)
}
\arguments{
\item{fit}{A \link{gbtm_fit} object.}

\item{observed}{Logical; overlay observed per-group occasion means
(default \code{TRUE}).}

\item{n}{Number of time grid points for the fitted lines.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Draws each group's fitted trajectory over time and, optionally, overlays the
observed mean outcome for the subjects assigned to that group at each
occasion. Requires the \pkg{ggplot2} package.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, paste0("y", 1:10),
                  paste0("t", 1:10), id = "id", family = "binomial")
if (requireNamespace("trajeR", quietly = TRUE) &&
    requireNamespace("ggplot2", quietly = TRUE)) {
  fit <- fit_gbtm(spec, n_groups = 4, degrees = rep(3, 4), method = "L")
  plot_trajectories(fit)
}
}
}
