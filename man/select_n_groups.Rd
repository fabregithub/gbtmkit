% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{select_n_groups}
\alias{select_n_groups}
\title{Stage 2: select the number of groups}
\usage{
select_n_groups(
  spec,
  engine = gbtm_engines(),
  candidates = 2:6,
  degree = 3L,
  degrees = NULL,
  method = NULL,
  by = c("bic", "aic"),
  hessian = FALSE,
  itermax = 100L,
  seed = NULL,
  ...
)
}
\arguments{
\item{spec}{A \link{gbtm_spec}.}

\item{engine}{Engine name; see \code{\link[=gbtm_engines]{gbtm_engines()}}.}

\item{candidates}{Integer vector of group numbers to try (default \code{2:6}).}

\item{degree}{Single polynomial degree applied to every group (default \code{3},
cubic). Ignored when \code{degrees} is supplied.}

\item{degrees}{Optional list, one integer vector per candidate, for full
control of the shape at each group number.}

\item{method}{Estimation method to use (e.g. the winner of
\code{\link[=select_algorithm]{select_algorithm()}}).}

\item{by}{Criterion to minimise, \code{"bic"} (default) or \code{"aic"}.}

\item{hessian, itermax, seed, ...}{Passed to \code{\link[=gbtm_fit]{gbtm_fit()}}.}
}
\value{
A \code{gbtm_selection} object with \verb{$table}, \verb{$best} (the chosen number
of groups), and the fitted models.
}
\description{
Fits the model for each candidate number of groups and picks the one with the
lowest BIC. Each candidate uses a polynomial degree of \code{degree} for every
group unless a per-candidate \code{degrees} list is supplied. The candidate fits
are independent and run in parallel under a \code{\link[future:plan]{future::plan()}} when the
future.apply package is installed.
}
\examples{
\donttest{
data("sim_binary", package = "gbtmkit")
spec <- gbtm_spec(sim_binary, paste0("y", 1:10),
                  paste0("t", 1:10), id = "id", family = "binomial")
select_n_groups(spec, candidates = 2:5, degree = 2)
}
}
